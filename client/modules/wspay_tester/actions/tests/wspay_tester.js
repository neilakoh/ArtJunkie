const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../wspay_tester';

describe('wspay_tester.actions.wspay_tester', () => {
  it('should do something');
});
